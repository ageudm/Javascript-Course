// Esturtura de controlo

const x = 10;
const y = 5;


// uma operação condional seve para dar resposta a uma certa pergunta na qual a resposta é ezecutada enquanto a pegunta for verdaderira
if (x >= y) {
    console.log(`${x} é maior ou igual ${y}`);
};


if (x === y) {
    console.log(`${x} é totalmente igual a ${y}`);
} else {
    console.log(`${x} não é totalmente igual a ${y}`);
}

if (x !== y) {
    const z = 20;
    console.log(`${z} é iguala 20`);
}

// shorthand ou forma simpes de escrever uma operação condicional
if (x >= y) console.log(`X é maior que Y`);
else console.log(`X não é maior que Ysd`);